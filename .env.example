# ============================================
# Configuration Production - Coupe de l'Universite
# ============================================
# Copier ce fichier vers .env et remplir les valeurs
#   cp .env.example .env
#
# IMPORTANT: Ne jamais commiter le fichier .env !

# ============================================
# DOMAINE
# ============================================
# Domaine principal (sans https://)
# Sous-domaines generes automatiquement:
#   - coupedeluniversite.fr         -> Frontend Public
#   - admin.coupedeluniversite.fr   -> Frontend Admin
#   - api.coupedeluniversite.fr     -> Backend API
#   - traefik.coupedeluniversite.fr -> Dashboard Traefik (optionnel)
DOMAIN=coupedeluniversite.fr

# ============================================
# SECRETS (OBLIGATOIRES - A CHANGER!)
# ============================================
# Cle secrete pour le backend (JWT, etc.)
# Generer avec: openssl rand -hex 32
SECRET_KEY=CHANGE_ME_IN_PRODUCTION_USE_OPENSSL_RAND

# Secret pour NextAuth (authentification frontend-admin)
# Generer avec: openssl rand -base64 32
AUTH_SECRET=CHANGE_ME_IN_PRODUCTION_USE_OPENSSL_RAND

# ============================================
# AUTHENTIFICATION ADMIN
# ============================================
# Identifiants pour l'interface d'administration
ADMIN_USERNAME=admin
ADMIN_PASSWORD=CHANGE_ME_STRONG_PASSWORD

STAFF_USERNAME=staff
STAFF_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# ============================================
# BASE DE DONNEES (optionnel)
# ============================================
# Chemin vers la base SQLite (dans le container)
# DATABASE_PATH=/app/data/coupe_ucl_2026.db
